import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FINITE — Life is finite. Make it count.",
  description:
    "FINITE is a minimalist life dashboard for iOS that makes time visible and helps you build momentum every day.",
  openGraph: {
    title: "FINITE — Life is finite. Make it count.",
    description:
      "A minimalist life dashboard that makes time visible and helps you build momentum every day.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} h-full antialiased`}>
      <body className="min-h-full bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
